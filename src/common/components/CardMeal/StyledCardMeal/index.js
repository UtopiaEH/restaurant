import styled from 'styled-components'
import CardMeal from "../index";

export const StyledCardMeal = styled(CardMeal)`
  .site-card-border-less-wrapper {
    background: #ececec;
    padding: 30px;
  }
`